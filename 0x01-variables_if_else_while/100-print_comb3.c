#include <stdio.h>
#include <stdlib.h>
/** main - Executing code for printing numbers with double digits
 * Return: 0 if exited succesffuly
 */
int main(void)
{
	int a, b;

	for (a='0';n a <= '9'; a++)
	{
		for (b = '0'; b<= '9'; b++)
		{
			if (c < a)
			{
				putchar(a);
				putchar(i);
				
				if (a != '8' || (a == '8' && i !=  '9' ))
				
					putchar(',');
					putchar(' ');
			}
		}
	}
	putchar('\n');
	return 0;
}
